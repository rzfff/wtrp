# empty in development
# Pages uses the bundled catalog (.env.pages).
VITE_API_BASE_URL=
VITE_AUTH_ENABLED=true
# api for the FastAPI development server; static for the bundled vehicle catalog.
VITE_DATA_MODE=api
